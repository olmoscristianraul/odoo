<?xml version="1.0" encoding="utf-8"?>
<odoo>

   <template id="partner_info" name="Argentinan partner">

        <!-- show document type -->
        <div t-attf-class="form-group #{error.get('l10n_latam_identification_type_id') and 'o_has_error' or ''} col-xl-6">
            <label class="col-form-label" for="l10n_latam_identification_type_id">Tipo de Documento</label>
            <select name="l10n_latam_identification_type_id"  t-attf-class="form-control #{error.get('l10n_latam_identification_type_id') and 'is-invalid' or ''}" t-att-readonly="None if can_edit_vat else '1'" t-att-title="None if can_edit_vat else 'Changing Identification type is not allowed once document(s) have been issued for your account. Please contact us directly for this operation.'" >
                <option value="">Tipo de Documento...</option>
                <t t-foreach="identification_types or []" t-as="identification_type">
                    <option t-att-value="identification_type.id" t-att-selected="identification_type.id == int(l10n_latam_identification_type_id) if l10n_latam_identification_type_id else identification_type.id == partner_identification_id">
                        <t t-esc="identification_type.name"/>
                    </option>
                </t>
            </select>
        </div>

        <!-- show afip responsibility -->
        <div t-attf-class="form-group #{error.get('l10n_ar_afip_responsibility_type_id') and 'o_has_error' or ''} col-xl-6">
            <label class="col-form-label" for="l10n_ar_afip_responsibility_type_id">Responsabilidad AFIP</label>
            <select name="l10n_ar_afip_responsibility_type_id"  t-attf-class="form-control #{error.get('l10n_ar_afip_responsibility_type_id') and 'is-invalid' or ''}" t-att-readonly="None if can_edit_vat else '1'" t-att-title="None if can_edit_vat else 'Changing AFIP Responsibility type is not allowed once document(s) have been issued for your account. Please contact us directly for this operation.'" >
                <option value="">Responsabilidad AFIP...</option>
                <t t-foreach="responsibility_types or []" t-as="responsibility_type">
                    <option t-att-value="responsibility_type.id" t-att-selected="responsibility_type.id == int(l10n_ar_afip_responsibility_type_id) if l10n_ar_afip_responsibility_type_id else responsibility_type.id == partner_responsibility_id">
                        <t t-esc="responsibility_type.name"/>
                    </option>
                </t>
            </select>
        </div>

    </template>

    <template id="portal_my_details" name="portal_my_details" inherit_id="portal.portal_my_details">
        <xpath expr="//input[@name='vat']/.." position="before">
            <t t-set="can_edit_vat" value="partner.can_edit_vat()"/>
            <t t-set="partner_responsibility_id" value="partner.l10n_ar_afip_responsibility_type_id.id"/>
            <t t-set="partner_identification_id" value="partner.l10n_latam_identification_type_id.id"/>
            <t t-call="l10n_ar_website_sale.partner_info"/>
        </xpath>
    </template>

    <template id="address_b2b" inherit_id="website_sale.address_b2b">
        <xpath expr="//input[@name='vat']/.." position="before">
            <t t-if="mode[1] == 'billing'" positon="inside">
                <t t-set="partner_responsibility_id" value="website_sale_order.partner_id.l10n_ar_afip_responsibility_type_id.id or 0"/>
                <t t-set="partner_identification_id" value="website_sale_order.partner_id.l10n_latam_identification_type_id.id or 0"/>
                <t t-call="l10n_ar_website_sale.partner_info"/>
            </t>
        </xpath>
    </template>

</odoo>
